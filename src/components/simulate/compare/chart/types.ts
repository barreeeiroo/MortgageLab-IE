/**
 * Shared types for compare chart components
 */

// Re-export chart types from the store
export type {
	CompareChartDataPoint,
	CompareSimulationData,
} from "@/lib/stores/simulate/simulate-compare-calculations";
